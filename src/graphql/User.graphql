query CurrentUser {
	getCurrentUser {
		_id
		displayName
		avatar
	}
}

query UserWallets {
	userWallets {
		type
		balance
	}
}

subscription AvailableBalances {
	availableBalances {
		amount
		balance {
			amount
			currency
		}
	}
}
