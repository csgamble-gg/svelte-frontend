query CurrentUser {
	getCurrentUser {
		_id
		displayName
		avatar
		wallets {
			type
			balance
		}
	}
}

query UserWallets {
	userWallets {
		type
		balance
	}
}

subscription AvailableBalances {
	availableBalances {
		amount
		balance {
			amount
			currency
		}
	}
}
