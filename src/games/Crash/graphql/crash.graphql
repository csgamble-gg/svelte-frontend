subscription CrashSubscription {
	crashGame {
		_id
		status
		elapsed
		multiplier
		startsAt
	}
}

query CrashInitial {
	crashInitial {
		crashGame {
			_id
			status
			startsAt
			elapsed
		}
	}
}

mutation CreateCrashBet {
	
}